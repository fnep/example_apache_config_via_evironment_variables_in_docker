<VirtualHost *:80>

    # Default server configuration
    ServerName "${SERVER_NAME}"
    ServerAdmin webmaster@localhost

    # Grant access to the web server
    <Directory />
        Require all granted
    </Directory>

    # DocumentRoot defined by environment variable
    DocumentRoot ${DOCUMENT_ROOT}

</VirtualHost>
